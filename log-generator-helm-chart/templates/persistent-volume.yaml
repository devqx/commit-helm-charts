apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.volumeMount.name }}

spec:
  capacity:
    storage: {{ .Values.volumeMount.capacity.storage }}
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: {{ .Values.volumeMount.path }}
